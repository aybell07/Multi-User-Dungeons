This is a short program that reads rooms formatted like the provided file (room1.txt) and allows the user to explore said rooms. 

Compile the program and then run the executable with the text file as the next entry in the command line.
